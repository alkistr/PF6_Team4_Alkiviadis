@model IEnumerable<PF6_Team4_Core.ViewModels.UserVM>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FirstName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.projectsUserVM)
            </td>
            @foreach (var listitem in Model)
            {
                <td>
                    @Html.DisplayFor(modelItem => listitem.projectsUserVM)
                </td>
            }*@
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserVMId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserVMId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserVMId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
