@model IEnumerable<PF6_Team4_Core.Models.UserLoggedIn>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserLoggedInId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserLoggedInId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserLoggedInId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
